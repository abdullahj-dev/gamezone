import BumpWrapper from "./BumpWrapper";

export const metadata = {
  title: "BUMP KNIGHTS | Overdrive",
  description: "The ultimate arena brawler.",
};

export default function BumpPage() {
  return <BumpWrapper />;
}